{
  "name": "ekantik-correction-dashboard",
  "private": true,
  "version": "2.0.0",
  "description": "Ekantik Correction Dashboard v2 — portal-integrated live correction-intelligence system",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspace @ekantik/correction-engine --workspace @ekantik/correction-worker",
    "build": "npm run build --workspaces --if-present"
  },
  "engines": {
    "node": ">=22"
  }
}
